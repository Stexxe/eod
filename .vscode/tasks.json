{
	"version": "2.0.0",
	"tasks": [
    {
		"label": "emmake-build",
		"type": "shell",
		"command": "emmake make",
		"group": {
			"kind": "build",
			"isDefault": true
		},
		"options": {
        	"cwd": "${workspaceFolder}/build"
      	},
		"problemMatcher": "$gcc"
    }
  ]
}